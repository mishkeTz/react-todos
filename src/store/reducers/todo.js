import * as actionTypes from '../actions';

const initialState = {
    todos: [],
    todo: '',
}

const reducer = (state = initialState, payload) => {
    switch (payload.type) {
        case actionTypes.ADD:
            
    }
}